{% extends "generic_show_form.html.twig" %}
{% import "components/form/fields_macros.html.twig" as fields %}

{% block form_fields %}
    {{ fields.smallTitle(__('This power value is used as default when the power of a monitor model is unknown', 'carbon')) }}
    {{ fields.numberField('power_consumption', item.fields['power_consumption'], __('Power', 'carbon'), {
        min: 0
    }) }}
{% endblock %}